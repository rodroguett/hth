<%= form_for(@dispatch) do |f| %>
  <% if @dispatch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dispatch.errors.count, "error") %> prohibited this dispatch from being saved:</h2>

      <ul>
      <% @dispatch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Despacho electrónico" %><br>
    <%= f.text_field :electronic_dispatch %>
  </div>
  <div class="field">
    <%= f.label :name, "Ticket de despacho" %><br>
    <%= f.text_area :dispatch_ticket %>
  </div>
  <div class="field">
    <%= f.label :name, "Fecha de despacho" %><br>
    <%= f.date_field :dispatch_date %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar" %>
  </div>
<% end %>