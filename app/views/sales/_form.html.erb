<%= form_for(@sale) do |f| %>
  <% if @sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sale.errors.count, "error") %> prohibited this sale from being saved:</h2>

      <ul>
      <% @sale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Precio %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :name, "Nota de venta" %><br>
    <%= f.text_area :sale_note %>
  </div>
  <div class="field">
    <%= f.label :name, "Orden de compra" %><br>
    <%= f.text_area :purchase_order %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar" %>
  </div>
<% end %>